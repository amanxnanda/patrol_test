name: patrol_test
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  patrol: ^3.3.0

flutter:
  uses-material-design: true

patrol:
  app_name: Patrol Test
  android:
    package_name: com.amannanda.patrol_test
  ios:
    bundle_id: com.amannanda.patrol_test